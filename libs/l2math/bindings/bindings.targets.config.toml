linux = [
    "x86_64-unknown-linux-gnu",
    # You may add more targets below, as long as they are supported by the Rust.
    # ALL target triples will be built and copied to the output directory.
]

windows = [
    "x86_64-pc-windows-msvc",
]

macos = [
    "x86_64-apple-darwin",
]

android = [
    "aarch64-linux-android",
    "armv7-linux-androideabi",
    "x86_64-linux-android",
    "i686-linux-android",
]

# You may add more targets below, as long as they are supported by the Rust.
# As an example:
# wasm = [ "wasm32-unknown-unknown" ]
